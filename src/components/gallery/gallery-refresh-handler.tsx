"use client"

export function GalleryRefreshHandler() {
  // Removed aggressive refresh handlers to prevent white page flashes
  // ISR (Incremental Static Regeneration) with 10-minute revalidation
  // handles gallery data updates automatically without UI disruption
  
  return null // This component doesn't render anything
}